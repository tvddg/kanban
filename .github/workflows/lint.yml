name: Code Quality

on:
    pull_request: 
        branches: [ main ]

jobs:
    eslint:
        name: Run ESLint
        runs-on: ubuntu-latest

        steps:
          - name: Checkout code
            uses: actions/checkout@v7

          - name: Setup Node.js
            uses: actions/setup-node@v7
            with: 
                node-version: 'latest'
                cache: 'npm'
        
          - name: Install dependencies
            run: npm ci
        
          - name: Run ESLint
            run: npm run lint